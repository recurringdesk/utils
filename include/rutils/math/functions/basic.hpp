#ifndef BASIC_GUARD
#define BASIC_GUARD

#include <rutils/core/api.hpp>

namespace Recurring::Math
{
    template <typename T>
    T square_root (T value);

    template <typename T>
    T pow (T base, T exponent);
} // namespace Recurring::Math

#endif
