#ifndef BASIC_GUARD
#define BASIC_GUARD

namespace Sixty::Math
{
    template <typename T>
    T square_root (T value);

    template <typename T>
    T pow (T base, T exponent);
} // namespace Sixty::Math

#endif
